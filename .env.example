# Flask runtime configuration (.env)
# Copy this file to `.env` and adjust as needed for your machine.

# Run in development mode (enables auto-reload, debugger)
FLASK_ENV=development
# Alternatively: FLASK_DEBUG=1

# Server bind
HOST=127.0.0.1
PORT=5000

# Path to CSV (absolute or relative to project root)
CSV_PATH=All_Diets.csv

# Optional: Allow all CORS (already permissive by default)
# CORS_ORIGINS=*
